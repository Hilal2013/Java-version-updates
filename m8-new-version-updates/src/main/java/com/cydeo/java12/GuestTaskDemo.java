package com.cydeo.java12;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;

public class GuestTaskDemo {
    public static void main(String[] args) {
//Map<String,Integer> list=     GuestData.getAll().stream()
      //  .filter(Guest::isParticipating)
       // .map(each->each.getName())
               // .collect(Collectors.summingInt(Guest::getParticipantsNumber))
                      //  .collect(Collectors.toMap(Collectors.mapping(Guest::getName,Collectors.toList()),Collectors.summingInt(Guest::getParticipantsNumber)));



      //  .collect(Collectors.toList());

     //   System.out.println(list);

    }
}
